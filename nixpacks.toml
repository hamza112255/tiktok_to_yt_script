[phases.setup]
aptPkgs = ['ffmpeg', 'libsm6', 'libxext6', 'libgl1-mesa-glx', 'fonts-dejavu-core']

[phases.install]
cmds = ['pip install -r requirements-railway.txt']

[start]
cmd = 'python -u railway_runtime_setup.py && python -u all_platforms_youtube.py'
